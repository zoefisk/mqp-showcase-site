import CaseStudySpecificationPreview from "@/components/case-study/CaseStudySpecificationPreview";

export const sectionTitle = "Stimulus / Specification Preview";

export const intro =
    "This section uses the current Vega-based editor in a read-only configuration as a specification-oriented companion to the replication work. A dedicated D3 viewer can be added later as the recreated stimuli are finalized.";

export const previewTitle = "Creatinine Gradient";

export const previewDescription =
    "Reference range number line with gradient styling";

<CaseStudySpecificationPreview
    sectionTitle={sectionTitle}
    intro={intro}
    previewTitle={previewTitle}
    previewDescription={previewDescription}
/>
